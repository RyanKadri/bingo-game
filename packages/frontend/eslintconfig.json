{
  "extends": ["react-app"],
  "rules": {
    "no-restricted-globals": "false",
    "jest/valid-expect-in-promise": "off"
  }, 
  "overrides": [
    {
      "files": ["**/*.ts?(x)"],
      "rules": {
        "additional-typescript-only-rule": "warn",
        "jest/valid-expect-in-promise": "off"
      }
    }
  ]
}